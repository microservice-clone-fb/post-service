server:
  port: 5004
  servlet:
    context-path: /post

spring:
  application:
    name: post-service
  data:
    mongodb:
      uri: mongodb://localhost:30000/post-service?authSource=admin

app:
  services:
    profile:
      url: http://localhost:5002/profile
